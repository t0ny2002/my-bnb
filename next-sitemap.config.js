/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://crownstonequarters.com', // 👈 replace with your domain
    generateRobotsTxt: true, // also creates robots.txt for you
    sitemapSize: 5000,
  };
  